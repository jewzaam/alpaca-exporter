# Generated By: Cursor (Claude Sonnet 4.5)
name: Lint

on:
  pull_request:
    branches: [ main, auto-mode ]
  push:
    branches: [ main, auto-mode ]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"

    - name: Run linting
      run: |
        make lint


